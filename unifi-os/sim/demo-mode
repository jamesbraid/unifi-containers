#!/usr/bin/env python3
# run-parts hook: seed the bundled Network App's simulation-mode keys before
# systemd starts it. Logic and rationale live in the shipped package, which
# the base image puts on PYTHONPATH; this is only the name run-parts finds.
import sys

from unifi_runtime.entrypoint import demo

sys.exit(demo.main())
