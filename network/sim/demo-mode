#!/usr/bin/env python3
# Init hook: seed simulation mode before the controller starts, so demo sites,
# devices and networks exist and no setup wizard runs. Logic and rationale live
# in the shipped package; this is only the name the hook runner finds.
import sys

from unifi_runtime.entrypoint import demo

sys.exit(demo.main())
