when:
  - event: push
    branch: [main, rc]

steps:
  mint-tag:
    image: alpine/git
    environment:
      FORGEJO_TOKEN:
        from_secret: forgejo_ci_token
    commands:
      - ./scripts/mint-tag.sh
